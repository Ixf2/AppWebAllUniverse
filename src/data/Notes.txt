Cada vez que se haga un 
- git add .
- git commit -m "[Cualquier info]"
- git push origin [rama]

Hay que hacer
- npm run build //Para que se guarde los cambios. Como  git add .
- firebase deploy --only hosting //Para que suba los cambios. Como git commit mezclado con git push,
para que termine de subirse